# Acunetix XML file parser
Extracts XSS or SQLi injection points from Acunetix `.xml` files.

Pulls files from `./scans/`, will loop through all present
outputs as `.csv` in `./outputs/` with a relevant filename.
